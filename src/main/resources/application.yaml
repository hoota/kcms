server:
  port: ${KCMS_HTTP_PORT:8080}
  address: 0.0.0.0
  servlet:
    context-path: /
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  compression:
    enabled: false

cms:
  url-base: ${KCMS_URL_BASE:http://localhost:${KCMS_HTTP_PORT:8080}}
  lang: ${KCMS_LANG:en}
  auth:
    username: admin
    password: ${KCMS_AUTH_PASS:qqq}

spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  datasource:
    hikari:
      jdbc-url: ${KCMS_DB_URL}
      username: ${KCMS_DB_USER}
      password: ${KCMS_DB_PASSWORD}
      driver-class-name: org.postgresql.Driver
      maximum-pool-size: ${KCMS_DB_POOL_MAX:5}
      minimum-idle: ${KCMS_DB_POOL_MIN:1}
      idle-timeout: 60000
      pool-name: ${KCMS_DB_POOL:ll-db-pool}
  liquibase:
    change-log: classpath:/liquibase/changelog-master.yaml
    drop-first: false
    enabled: true
    url: ${spring.datasource.hikari.jdbc-url}
    user: ${spring.datasource.hikari.username}
    password: ${spring.datasource.hikari.password}

  # ===============================
  # = JPA / HIBERNATE
  # ===============================
  jpa:
    #    show-sql: true
    properties.hibernate:
      query.plan_cache_max_size: 256
      criteria.literal_handling_mode: bind
      # Disabled exception java.sql.SQLFeatureNotSupportedException:
      # Method org.postgresql.jdbc.PgConnection.createClob() is not yet implemented.
      temp.use_jdbc_metadata_defaults: false
      # Allows Hibernate to generate SQL optimized for a particular DBMS
      dialect: org.hibernate.dialect.PostgreSQL9Dialect
      # Use spring.jpa.properties.* for Hibernate native properties (the prefix is
      # stripped before adding them to the entity manager).
      #        show-sql: true
      ddl-auto: none
      # Naming strategy
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

access-control:
  allow-origin: '*'
  allow-methods: 'POST,GET,DELETE,PUT,OPTIONS'
  allow-headers: 'Content-Type,Authorization'
  allow-credentials: true
